var index = 1 
while (index < 10)  {
    if (index%2==0)
    console.log ("el numero es  " + index + " es par")
    alert ("el valor de index es: " + index)
    index++
}
alert ("salimos del loop con el valor deeel loop igual a " + index)